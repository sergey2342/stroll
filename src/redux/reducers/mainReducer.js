import { mainInitialState } from '../initialStates'


const mainReducer = (state = mainInitialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default mainReducer